#!/usr/bin/env python3
from pwn import *

p = process('./')

print(p.recv(1024))
print(p.recv(1024))
print(p.recv(1024))
print(p.recv(1024))
print(p.recv(1024))
print(p.recv(1024))
print(p.recv(1024))
